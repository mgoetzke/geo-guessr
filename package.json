{
    "name": "geo-suessr",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"cd backend && node index.ts\" \"cd client && npm start\""
    },
    "dependencies": {
        "concurrently": "^6.0.0"
    }
}